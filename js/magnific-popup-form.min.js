$(document).ready(function() {
	
	var id = "";
	$('a.popup-with-form').click(function() { 
		id = $(this).attr('itemId');
	});
	
	$('.popup-with-form').magnificPopup({
		
		type: 'inline',
		preloader: false,
		focus: '#name',

		// When elemened is focused, some mobile browsers in some cases zoom in
		// It looks not nice, so we disable it:
		callbacks: {
			beforeOpen: function() {
				//$('#test-form').attr("itemIdq",id);				
				//html = '?php  $n = "'+id+'"; ?></div>';
				
				//$("#myid").html(html);
				if($(window).width() < 700) {
					this.st.focus = false;
				} else {
					this.st.focus = '#name';
				}
			}
		}
		
	});
});
